default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") %>
  host: <%= ENV.fetch("BACKEND_DATABASE_HOST") %>
  port: <%= ENV.fetch("BACKEND_DATABASE_PORT") %>
  database: <%= ENV.fetch("BACKEND_DATABASE_NAME") %>
  username: <%= ENV.fetch("BACKEND_DATABASE_USER") %>
  password: <%= ENV.fetch("BACKEND_DATABASE_PASSWORD") %>

development:
  <<: *default
  database: <%= ENV.fetch("BACKEND_DATABASE_NAME") %>

test:
  <<: *default
  database: backend_test

production:
  <<: *default
  database: <%= ENV.fetch("BACKEND_DATABASE_NAME") %>
